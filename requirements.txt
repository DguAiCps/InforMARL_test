# Core dependencies
gymnasium
matplotlib==3.10.3
numpy==2.3.1
pyyaml

# PyTorch with CUDA support (choose one based on your CUDA version)

# For Server: CUDA 12.1+ (RTX 4070 Ti, CUDA 13.0)
--index-url https://download.pytorch.org/whl/cu121
torch
torchvision

# For Local Testing: CUDA 11.8 (older GPUs)
# --index-url https://download.pytorch.org/whl/cu118
# torch
# torchvision

# For CPU only (local testing without GPU)
# --index-url https://download.pytorch.org/whl/cpu
# torch
# torchvision

# Reset to default PyPI for other packages
--index-url https://pypi.org/simple

# Graph neural networks (install from default PyPI)
torch-geometric
